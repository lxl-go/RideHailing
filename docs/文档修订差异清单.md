# 网约车文档框架对齐修订差异清单

> 修订日期：2026-07-28  
> 修订范围：《网约车系统（乘客端）需求分析文档》《网约车系统（司机端）需求分析文档》《网约车系统（管理端）需求分析文档》《网约车系统技术评审文档》  
> 修订原则：基于当前已搭建标准化框架补充文档事实，不修改 Kratos/GVA 底层框架、DDD 分层目录、pkg 公共包和网关标准目录。

## 一、数据库层差异

| 文档原要求 | 当前框架现有实现 | 文档修订方案 |
|-----------|----------------|-------------|
| 技术评审描述了 01~11 工单全量核心表 | 当前可确认落地的业务模型集中在 `carpool_certification_audit`、`carpool_vehicle_info`、`carpool_trip`、乘客端 `carpool_order`、`carpool_review` | 在技术评审第十四章补充“已落地业务表与模型基线”，将全量表描述调整为目标架构，当前表作为一期基线 |
| `carpool_trip` 在不同端侧存在字段口径差异 | 管理端模型使用 publisher/originName/destName/shareCost，乘客服务模型使用 driver/origin/destination/price | 技术评审新增字段命名差异风险，要求后续统一数据字典或增加转换层 |
| 退款、营销、派单、班车等表默认视为完整落地 | 当前工作区未看到这些服务的真实表模型 | 三份需求文档和技术评审均标注为“页面/目标架构已描述，真实接口和表结构待补齐” |

## 二、后端代码差异

| 文档原要求 | 当前框架现有实现 | 文档修订方案 |
|-----------|----------------|-------------|
| 三端 API 均为独立部署单元且功能完整 | 管理端 carpool 审核/行程治理已落地；乘客端 trip/order/review/gateway 已落地；司机端只有前端接口契约，未见独立 driver-api 源码 | 技术评审第十四章明确三端当前落地边界，司机端标注为“契约已定义，源码待落盘或共享服务承载” |
| JWT、Casbin、trace、限流、缓存、事务、消息队列全部已落地 | 管理端 JWT/Casbin/trace/审计较完整；乘客端 JWT 和 ctx 透传已具备；Redis 锁、MQ 工具存在但业务接入不足 | 技术评审改为“已有能力 + 当前缺口 + 验收口径”三列描述 |
| 下单事务已满足高并发一致性 | 乘客端创建订单有事务闭包，但仓储仍绑定自身 DB，存在事务未完全绑定同一 tx 的风险 | 技术评审新增事务检查项：事务闭包内仓储必须使用同一 tx |
| 全量接口均可真实联调 | 当前真实接口主要是管理端 WO-01 和乘客端顺风车主链路 | 三端需求文档补充真实接口边界表，避免把 mock/placeholder 视为真实联调 |

## 三、前端页面差异

| 文档原要求 | 当前框架现有实现 | 文档修订方案 |
|-----------|----------------|-------------|
| 管理端 01~11 工单页面均应完整联调 | `rideHailing/workorders` 中 WO-01 调真实接口，其他工单走 `callPlaceholderApi`；drivers/passengers/orders 页面使用 mockData | 管理端需求第十四章补充页面修订表，区分页面原型验收和真实业务验收 |
| 乘客端功能均应对应真实服务 | 乘客端页面覆盖 WO-01/02/03/04/07/08/10/11，但除顺风车主链路外多为 mock/占位 | 乘客端需求第八章补充页面验收表与一期真实接口边界 |
| 司机端派单、位置、收入等功能默认真实联调 | 司机端 Web/UniApp 页面已具备，API 契约已定义，但后端源码未见 | 司机端需求第七章补充“页面已具备、driver-api 待落地”说明 |

## 四、技术规范差异

| 文档原要求 | 当前框架现有实现 | 文档修订方案 |
|-----------|----------------|-------------|
| 统一错误码已经完全落地 | GVA 使用 `code/data/msg`，乘客网关存在 HTTP status + `error{code,message}` 格式 | 技术评审第十四章要求后续统一项目级错误码表，并覆盖两类现状 |
| 所有缓存、锁、MQ、压测能力均已完成业务接入 | 公共工具较齐，但业务接入范围有限，压测脚本目录未确认 | 技术评审新增压测产出路径与锁 key/TTL 验收口径 |
| 文档默认描述最终全量系统 | 当前代码是标准框架 + 部分业务主链路 | 四份文档均追加“基于当前框架的补充修订”章节，明确一期边界与后续补齐项 |
| 三端配置中心口径不统一 | 管理端原为 `nacos:` 嵌套结构，乘客端原为顶层 Nacos 字段；截图中的实际配置为 `public / DEFAULT_GROUP / ride-car` | 已统一本地 bootstrap 为 `nacos:` 嵌套结构，Data ID 统一 `ride-car`，本地 `config.yaml` 仅作为降级兜底 |

## 五、最终验收核对清单

| 核对项 | 状态 | 说明 |
|-------|------|------|
| 三份需求文档补充当前框架事实 | 已完成 | 乘客端第八章、司机端第七章、管理端第十四章 |
| 技术评审补充当前代码事实 | 已完成 | 技术评审第十四章 |
| 标注真实接口与 mock/placeholder 差异 | 已完成 | 三端需求与技术评审均已补充 |
| 保持底层框架不修改 | 已完成 | 本次只修改 Markdown 文档 |
| 输出统一差异清单 | 已完成 | 当前文件 |
| Nacos bootstrap 配置统一 | 已完成 | 管理端与乘客端 `configs/nacos.yaml` 已统一指向 `ride-car` |
| 后续代码整改范围 | 待后续任务 | 司机端 API、事务 tx 绑定、统一错误码、Redis 锁接入、压测脚本等需单独执行代码任务 |

## 六、2026-07-29 工单 01~05 验证与生产化缺口补充

| 修订对象 | 本次新增内容 | 修订原因 |
|---------|-------------|---------|
| 管理端需求文档 | 新增 14.6、14.7，记录 WO-01~WO-05 当前验证结论、权限异常体验、资源归属、审计、服务不可用降级和 WO-06~WO-11 后续边界 | 目录和框架已调整，管理端 1~5 已开发，需要把真实验证状态与后续生产化验收口径写清 |
| 乘客端需求文档 | 新增 8.6，补充本人订单/行程/评价归属、权限异常提示、服务不可用体验和后续工单真实接口验收边界 | 需求文档需要补上“是不是自己的订单/行程/评价”的业务安全口径 |
| 司机端需求文档 | 新增 7.6，补充本人行程、车辆、认证资料、接单归属、弱网和服务异常体验 | 司机端后续真实接口接入前，需要明确本人资源归属与派单越权拦截要求 |
| 技术评审文档 | 新增 14.6~14.9，补充权限缓存与降级、资源归属、gRPC 标准化、AutoMigrate 当前口径、日志追踪指标、根级工程配置、trip-service 依赖卫生、01~05 验证矩阵、06~11 开发进入条件 | 生产级核心缺口需从“目标架构描述”落到“当前事实 + 目标方案 + 验收标准 + 优先级” |

| 当前验证项 | 结果 | 后续处理 |
|-----------|------|---------|
| 管理端 carpool WO-02~WO-05 后端测试 | 通过 | 进入真实接口验收和生产化权限/归属补测 |
| auth-service、gateway-service、order-service、driver-service、passenger-service、review-service 关键测试 | 通过 | 保持为后续 06~11 开发的回归基线 |
| trip-service 测试 | 通过 | 已完成依赖整理，当前保留 `AutoMigrate` 自动迁移表结构 |
| admin-platform/web 构建 | 通过 | 大 chunk 与 npm 配置警告进入后续性能优化项 |
| 根 go.work / package.json | 已修正旧目录引用 | 后续补充根级一键校验脚本 |

## 七、2026-07-29 生产化底座首批代码整改补充

| 修订对象 | 本次新增内容 | 修订原因 |
|---------|-------------|---------|
| `gateway-service` | 新增权限短 TTL 缓存、auth-service 连续失败熔断、配置化权限策略，并补充缓存命中、TTL 过期、熔断打开测试 | 降低每次鉴权都远程调用 auth-service 的生产风险 |
| `pkg/grpcx`、gateway 下游 client 与各微服务 gRPC server | 新增统一 `DialInsecure`、默认客户端超时、metadata/traceparent 透传、读接口默认重试、业务错误到 gRPC status 映射、client/server 调用日志；gateway 聚合 client 改为统一入口，auth/trip/order/passenger/driver/review server 接入统一拦截器 | 为 WO-06~WO-11 后续服务调用统一超时、metadata、trace id、错误码和重试规则奠定基础 |
| `trip-service` | 撤回版本化 SQL migration runner 接入，恢复 `NewDB` 中直接执行 `AutoMigrate(&tripModel{})` | 当前阶段需要由 `AutoMigrate` 自动将表结构迁移到数据库 |
| `apps/` 前端目录 | 新增 `apps/passenger-uni-app`、`apps/driver-uni-app`、`apps/admin-web` 目标目录，并将根 workspace 纳入 `apps/*` | 对齐根目录前端应用结构预期，后续 WO-06~WO-11 页面在该结构下继续补齐 |
| `order-service` | 新增乘客访问他人订单、司机处理他人行程订单的越权回归测试 | 资源归属校验是后续生产级安全的核心前置 |

## 八、2026-07-29 WO-07 营销管理接入补充

| 修订对象 | 本次新增内容 | 修订原因 |
|---------|-------------|---------|
| 管理端需求文档 | 在 14.6 当前验证补充中新增 WO-06 数据分析、WO-07 营销当前开发结论；该阶段曾将 08 至 11 作为后续边界，现已由第十节更新为 WO-10~WO-11 | WO-06、WO-07 已具备管理端接口和页面联调基础，需求文档需从“待开发依据”更新为“已接入但需生产化补强” |
| 技术评审文档 | 将 14.7 验证矩阵升级为 WO-01~WO-07，补充 WO-07 营销后端测试、AutoMigrate 接入、管理端前端构建与页面资产说明；该阶段曾将 08 至 11 作为后续开发条件，现已由第十节更新为 WO-10~WO-11 | 技术评审需体现当前真实代码状态，同时保留领券防刷、库存幂等、退款退券、预算风控等生产级验收重点 |
| `admin-platform/server` | 新增营销模型、请求、服务、接口、路由与启动迁移注册；覆盖优惠券模板、发放、核销、活动列表、推荐奖励和导出任务 | WO-07 管理端后端闭环需要真实业务服务，不以 mock 替代验收 |
| `admin-platform/web` | 新增 WO-07 营销 API、页面、静态路由、动态菜单和工单中心入口 | 对齐根目录 `apps/admin-web` 前端结构，让营销管理可从管理端真实入口访问 |

## 九、2026-07-30 WO-08 性能压测底座接入补充

| 修订对象 | 本次新增内容 | 修订原因 |
|---------|-------------|---------|
| 管理端需求文档 | 在工单矩阵、当前接口边界、页面状态和 14.6 验证补充中标记 WO-08 性能底座已接入；该阶段曾将后续边界调整为九至十一工单，现已由第十节更新为 WO-10~WO-11 | WO-08 已有管理端性能接口和页面，不应继续写成待开发；真实 WebSocket、位置流和派单推送仍归 WO-11 |
| 技术评审文档 | 补充 WO-08 当前落地说明、性能脚本/报告路径、三端页面验收口径和 14.7 验证矩阵 | 技术评审需要反映 `docs/performance/scripts`、`docs/performance/reports`、`/carpool/performance/*`、移动端性能页的真实状态 |
| `admin-platform/server` | 新增性能报告、验收场景、runtime 快照、导出任务相关模型、服务、接口、路由和 `AutoMigrate` 注册 | 管理端需要可录入、筛选、汇总和追踪 WO-08 压测报告 |
| `admin-platform/web` | 新增 WO-08 性能 API、页面、静态路由、动态菜单和工单中心入口 | 管理端可直接进入性能压测页面查看 KPI、runtime、报告和验收目标 |
| `apps/passenger-uni-app`、`apps/driver-uni-app` | 性能页由固定指标展示调整为报告式验收页 | 为 WO-11 接入真实 WebSocket/位置服务后的移动端复测预留统一展示口径 |
| `docs/performance/` | 新增 admin HTTP、乘客 WS 地图、司机位置上报 k6 样例脚本和 3 份 sample JSON 报告 | 为后续真实压测提供可追溯产物目录和报告字段模板 |

## 十、2026-07-30 WO-09 GVA 框架治理底座接入补充

| 修订对象 | 本次新增内容 | 修订原因 |
|---------|-------------|---------|
| 管理端需求文档 | 标记 WO-09 GVA 治理底座已接入，并将后续边界调整为 WO-10~WO-11 | WO-09 已具备管理端接口和页面，不应继续写成待开发 |
| 技术评审文档 | 补充 WO-09 路由治理、权限审计、多数据源健康、定时任务治理和验证结果 | 技术评审需体现当前真实代码状态，并保留 Redis Pub/Sub/Kafka 路由刷新为后续生产化补强 |
| `admin-platform/server` | 新增 GVA 治理服务、接口和路由 | 管理端需要统一查看动态路由、审计、多数据源和定时任务治理状态 |
| `admin-platform/web` | 新增 WO-09 API、页面、静态路由、动态菜单和工单中心入口 | 管理端可从工单中心进入 GVA 治理页面 |
## 2026-07-31 WO-10/WO-11 开发差异

- 新增共享 Coze 私有接口客户端：`pkg/cozex`，按用户提供 curl 结构固定请求，不使用官方 `api.coze.cn`。
- 新增管理端 AI 模块：AI 会话、路线规划、积水上报、降级模板、API/router、AutoMigrate。
- 新增管理端派单模块：派单审计、规则配置、司机位置点、实时消息、订单 AI 上下文字段、API/router、AutoMigrate。
- 新增 admin-web 页面：`workorder10/ai`、`workorder11/dispatch`，并更新静态路由、动态菜单和工单总览。
- 新增乘客端/司机端 API wrapper 与页面真实请求接入，移除 AI 预警、接单、积水上报、轨迹等页面的硬编码成功逻辑。
- 新增 gateway 移动端最终路径入口；AI/定位/轨迹服务未真实接入时返回明确不可用状态，订单列表/接单复用现有 order-service。
- 保留事项：Coze `stream_run` 当前仍需用户在扣子部署中补齐后端 `TRAVEL_API_BASE_URL` 可达性或接口配置；后续再接真实 ai-service/location-service。
